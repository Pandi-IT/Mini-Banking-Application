# =========================================================================
#             FRONTEND PORTAL CONFIGURATION (TEMPLATE)
# =========================================================================

# Set to empty '' for relative paths in local development (proxied via proxy.conf.json)
# Set to your live backend URL (e.g. 'https://bankease-backend.onrender.com') when deploying frontend standalone
FRONTEND_BACKEND_API_URL=
